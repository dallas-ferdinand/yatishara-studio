{
  "name": "studio-ffmpeg-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild server.ts --bundle --platform=node --format=esm --outfile=dist/server.mjs",
    "start": "node dist/server.mjs"
  }
}
